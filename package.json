{
  "name": "notate",
  "private": true,
  "workspaces": ["apps/web"],
  "scripts": {
    "build": "npm run build -w @notate/web",
    "dev": "npm run dev -w @notate/web",
    "desktop": "npm run desktop -w @notate/web"
  }
}
